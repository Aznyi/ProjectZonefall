package com.zonefall.objective;

/**
 * Controls when an authored objective becomes visible and usable.
 */
public enum ObjectiveRevealMode {
    ROUND_START,
    FINAL_EXTRACTION,
    CUSTOM_TIME_REMAINING
}
