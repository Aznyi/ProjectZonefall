package com.zonefall.objective;

/**
 * Per-round activation rules for high-value objective candidates.
 */
public enum ObjectiveActivationMode {
    ALL_ACTIVE,
    RANDOM_ONE,
    RANDOM_TWO,
    RANDOM_COUNT
}
