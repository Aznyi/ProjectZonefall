package com.zonefall.loot.source;

/**
 * Controls how many authored loot sources become active each round.
 */
public enum LootActivationMode {
    ALL_ACTIVE,
    RANDOM_ONE,
    RANDOM_TWO,
    RANDOM_COUNT
}

