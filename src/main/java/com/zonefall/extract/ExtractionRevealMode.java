package com.zonefall.extract;

/**
 * Controls when selected extraction zones become visible and usable.
 */
public enum ExtractionRevealMode {
    ROUND_START,
    FINAL_EXTRACTION,
    CUSTOM_TIME_REMAINING
}

