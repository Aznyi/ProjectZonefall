package com.zonefall.extract;

/**
 * Controls how configured extraction zones become active each round.
 */
public enum ExtractionActivationMode {
    ALL_ACTIVE,
    RANDOM_ONE,
    RANDOM_TWO,
    RANDOM_COUNT
}

